Place your Google OAuth client credentials here.

1. Download from Google Cloud Console (Desktop app OAuth client)
2. Rename the file to `google_credentials.json`
3. See `docs/GMAIL_SETUP.md` for full setup steps

**Do not commit `google_credentials.json` to git.**
